---
phase: 10-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/opencode/src/user/user-context.ts
  - packages/opencode/src/server/user-auth.ts
  - packages/opencode/src/user/user-context.test.ts
  - packages/opencode/src/server/server.test.ts
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03]

must_haves:
  truths:
    - "TypeScript编译器拒绝处理匿名身份的代码"
    - "代码库中不存在匿名回退逻辑的引用"
    - "UserContext.get()直接返回Authenticated类型"
  artifacts:
    - path: "packages/opencode/src/user/user-context.ts"
      provides: "简化的Identity类型（仅Authenticated）"
      min_lines: 25
    - path: "packages/opencode/src/server/user-auth.ts"
      provides: "移除匿名返回路径的resolve函数"
      min_lines: 30
    - path: "packages/opencode/src/user/user-context.test.ts"
      provides: "仅包含认证测试的测试文件"
      min_lines: 20
  key_links:
    - from: "packages/opencode/src/user/user-context.ts"
      to: "Identity type"
      via: "type definition"
      pattern: "export type Identity = Authenticated"
    - from: "packages/opencode/src/server/user-auth.ts"
      to: "Authenticated"
      via: "return type"
      pattern: "return.*authenticated.*user_id"
---

<objective>
移除Identity类型中的Anonymous分支，清理所有匿名回退逻辑，强化类型系统使其拒绝处理匿名身份。

Purpose: Phase 9的中间件已确保所有请求都经过认证，因此可以安全移除匿名处理代码，通过类型系统提供编译时保障。
Output: 简化的类型定义和清理后的代码库，TypeScript编译器会拒绝任何尝试处理匿名状态的代码。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-code-cleanup/10-CONTEXT.md
@.planning/phases/09-authentication-enforcement/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>任务1: 简化user-context.ts类型定义</name>
  <files>packages/opencode/src/user/user-context.ts</files>
  <action>
1. 移除Anonymous类型定义（第9-12行）
2. 将Identity类型从联合类型改为直接等于Authenticated（第14行）: `export type Identity = Authenticated`
3. 移除ANONYMOUS_MISSING常量（第16行）
4. 简化UserContext.get()方法（第25-31行）: 直接返回`ctx.use()`，移除try-catch回退
5. 简化userID getter（第33-36行）: 直接返回`UserContext.get().user_id`，类型为`UserID`（非可选）
6. 移除authenticated getter（第38-40行）: 不再需要，始终为true

保持Context导入和provide方法不变。
</action>
<verify>
<automated>cd packages/opencode && bun typecheck 2>&1 | grep -E "(error|user-context)" || echo "PASS"</automated>
</verify>
<done>Identity类型仅为Authenticated，UserContext.get()返回Authenticated，userID为非可选类型</done>
</task>

<task type="auto">
  <name>任务2: 清理user-auth.ts匿名返回路径</name>
  <files>packages/opencode/src/server/user-auth.ts</files>
  <action>
1. 修改resolve()函数签名，返回类型改为`Promise<Authenticated>`
2. 移除第12行的匿名返回: `if (!key) throw new Error("API key missing")`
3. 移除第16行的匿名返回: `if (!valid(key)) { throttle(key, "malformed"); throw new Error("Invalid API key") }`
4. 移除第30行的匿名返回: `throttle(key, "unknown"); throw new Error("Unknown API key")`
5. 保留第25行的认证成功返回: `return { state: "authenticated" as const, user_id: row.user_id as UserID }`
6. 保留throttle()函数用于日志记录（第33-40行）

注意: 这些错误会被Phase 9的中间件捕获并转换为401响应，因此抛出错误是正确的行为。
</action>
<verify>
<automated>cd packages/opencode && bun typecheck 2>&1 | grep -E "(error|user-auth)" || echo "PASS"</automated>
</verify>
<done>resolve()仅返回Authenticated或抛出错误，无匿名返回路径</done>
</task>

<task type="auto">
  <name>任务3: 删除匿名相关测试</name>
  <files>packages/opencode/src/user/user-context.test.ts, packages/opencode/src/server/server.test.ts</files>
  <action>
**user-context.test.ts:**
1. 删除"defaults to anonymous when no context"测试（第5-9行）
2. 删除"provides anonymous identity"测试（第20-26行）
3. 删除"userID helper returns undefined for anonymous"测试（第28-31行）
4. 删除"authenticated helper"测试中的匿名部分（第38-40行）
5. 保留"provides authenticated identity"测试（第11-18行）
6. 保留"userID helper returns user_id for authenticated"测试（第33-36行）

**server.test.ts:**

1. 找到第21行的匿名状态检查: `if (identity.state === "anonymous")`
2. 移除该条件分支及其内容
3. 保留认证成功和401响应的测试逻辑

保持测试文件结构和其他测试不变。
</action>
<verify>
<automated>cd packages/opencode && bun test user-context.test.ts server.test.ts 2>&1 | tail -5</automated>
</verify>
<done>测试文件仅包含认证相关测试，无匿名测试用例</done>
</task>

</tasks>

<verification>
1. TypeScript编译通过，无类型错误
2. 所有测试通过
3. grep验证无Anonymous类型引用: `grep -r "Anonymous\|anonymous" packages/opencode/src/user/ packages/opencode/src/server/user-auth.ts`应仅返回日志消息和注释
4. Identity类型为单一类型，非联合类型
</verification>

<success_criteria>

- TypeScript编译器拒绝任何尝试处理`identity.state === "anonymous"`的代码
- UserContext.get()返回类型为Authenticated（非联合类型）
- UserContext.userID返回UserID（非可选）
- 代码库中无Anonymous类型定义
- 测试套件通过，仅包含认证测试
  </success_criteria>

<output>
完成后创建 `.planning/phases/10-code-cleanup/10-01-SUMMARY.md`
</output>
