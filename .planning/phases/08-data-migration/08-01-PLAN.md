---
phase: 08-data-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/opencode/src/session/session.sql.ts
  - packages/opencode/migration/*_make_user_id_required/migration.sql
autonomous: true
requirements:
  - MIGR-01
  - MIGR-02

must_haves:
  truths:
    - "查询所有会话不会遇到NULL user_id"
    - "数据库schema拒绝创建没有user_id的会话"
    - "迁移完成无数据丢失"
  artifacts:
    - path: "packages/opencode/src/session/session.sql.ts"
      provides: "SessionTable schema with user_id NOT NULL"
      contains: "user_id.*notNull"
    - path: "packages/opencode/migration/*_make_user_id_required/migration.sql"
      provides: "Migration SQL"
      min_lines: 5
  key_links:
    - from: "packages/opencode/src/session/session.sql.ts"
      to: "migration SQL"
      via: "drizzle-kit generate"
      pattern: "bun run db generate"
---

<objective>
将SessionTable.user_id列从nullable改为NOT NULL,确保所有会话都有有效的用户所有者。

Purpose: 为Phase 9的强制身份验证准备数据库,防止创建匿名会话
Output: Drizzle迁移文件,schema更新
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-data-migration/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新SessionTable schema添加user_id NOT NULL约束</name>
  <files>packages/opencode/src/session/session.sql.ts</files>
  <action>
在SessionTable定义中添加user_id列(如果不存在)或修改为NOT NULL:

```typescript
import type { UserID } from "../user/schema"

// 在SessionTable中添加:
user_id: text()
  .$type<UserID>()
  .notNull()
  .references(() => UserTable.id, { onDelete: "cascade" }),
```

位置: 在project_id之后,workspace_id之前
需要导入UserTable: `import { UserTable } from "../user/user.sql"`
需要导入UserID类型: `import type { UserID } from "../user/schema"`

同时在索引数组中添加user_id索引(如果不存在):

```typescript
index("session_user_idx").on(table.user_id)
```

  </action>
  <verify>
    <automated>cd packages/opencode && bun typecheck</automated>
  </verify>
  <done>SessionTable.user_id定义为notNull(),类型检查通过</done>
</task>

<task type="auto">
  <name>Task 2: 生成Drizzle迁移</name>
  <files>packages/opencode/migration/*_make_user_id_required/migration.sql</files>
  <action>
运行Drizzle Kit生成迁移:

```bash
cd packages/opencode
bun run db generate --name make_user_id_required
```

这将生成包含以下SQL的迁移:

- 处理NULL user_id的数据迁移(如果有)
- ALTER TABLE session修改user_id为NOT NULL

验证生成的SQL包含:

- 数据迁移步骤(UPDATE或DELETE NULL user_id行)
- ALTER TABLE语句修改约束
  </action>
  <verify>
  <automated>ls packages/opencode/migration/\*\_make_user_id_required/migration.sql</automated>
  </verify>
  <done>迁移文件已生成,包含ALTER TABLE语句</done>
  </task>

</tasks>

<verification>
1. Schema文件包含user_id NOT NULL定义
2. 迁移文件存在于migration目录
3. 类型检查通过(bun typecheck)
4. 迁移SQL包含ALTER TABLE语句
</verification>

<success_criteria>

- SessionTable schema定义user_id为NOT NULL
- Drizzle迁移文件已生成
- 类型检查无错误
- 迁移准备好应用到数据库
  </success_criteria>

<output>
After completion, create `.planning/phases/08-data-migration/08-01-SUMMARY.md`
</output>
