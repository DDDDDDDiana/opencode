---
phase: 05-session-derived-ownership-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - packages/opencode/src/server/routes/session.ts
  - packages/opencode/test/server/session-part-ownership.test.ts
autonomous: true
requirements:
  - SESS-07
  - SESS-08
must_haves:
  truths:
    - "Only the owning user can mutate or retrieve part-bearing message data through session-derived routes."
    - "Legacy anonymous sessions still expose their own message parts to anonymous callers, but never another user's data."
  artifacts:
    - path: packages/opencode/src/server/routes/session.ts
      provides: Part and message mutation handlers guarded by the shared session-root helper
    - path: packages/opencode/test/server/session-part-ownership.test.ts
      provides: Regression coverage for message/part ownership and anonymous fallback
  key_links:
    - from: packages/opencode/src/server/routes/session.ts
      to: packages/opencode/src/session/index.ts
      via: shared helper runs before Session.removeMessage/removePart/updatePart
      pattern: await .*Session\.get.*\n.*Session\.(removeMessage|removePart|updatePart)
    - from: packages/opencode/test/server/session-part-ownership.test.ts
      to: packages/opencode/src/server/server.ts
      via: x-opencode-api-key requests exercise UserContext middleware
      pattern: x-opencode-api-key
---

<objective>
Extend the same session-root guard to the remaining message/part mutation routes and add regression coverage that proves parts stay behind session ownership boundaries.

Purpose: Finish end-to-end protection for session-derived resources instead of only protecting read-only paths.
Output: Guarded delete/update handlers plus ownership regression tests for message parts.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-session-derived-ownership-closure/05-RESEARCH.md
@.planning/phases/05-session-derived-ownership-closure/05-01-SUMMARY.md
@packages/opencode/src/server/routes/session.ts
@packages/opencode/src/session/index.ts
@packages/opencode/src/server/server.ts
@packages/opencode/src/user/user-context.ts

<interfaces>
From `packages/opencode/src/session/index.ts`:

```ts
export const removeMessage = fn(z.object({ sessionID: SessionID.zod, messageID: MessageID.zod }), ...)
export const removePart = fn(z.object({ sessionID: SessionID.zod, messageID: MessageID.zod, partID: PartID.zod }), ...)
export const updatePart = fn(MessageV2.Part, async (part) => ...)
```

From `packages/opencode/src/server/server.ts`:

```ts
.use(async (c, next) => {
  const key = c.req.header("x-opencode-api-key")
  const identity = await resolve(key)
  return UserContext.provide(identity, () => next())
})
```

From `packages/opencode/src/user/user-context.ts`:

```ts
export const UserContext = {
  provide<R>(identity: Identity, fn: () => R): R,
  get(): Identity,
  get userID(): UserID | undefined,
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: guard remaining message and part route handlers</name>
  <files>packages/opencode/src/server/routes/session.ts</files>
  <action>Reuse the shared helper from plan `05-01` in `DELETE /:sessionID/message/:messageID`, `DELETE /:sessionID/message/:messageID/part/:partID`, and `PATCH /:sessionID/message/:messageID/part/:partID` before any mutation runs. Keep handler behavior otherwise unchanged, including the existing body/id consistency check for part patch requests. Do not add separate ownership queries inside `Session.removeMessage`, `Session.removePart`, or `Session.updatePart`; Phase 5 wants one shared route-level session-root guard.</action>
  <verify>
    <automated>bun test test/server/session-part-ownership.test.ts --timeout 30000</automated>
  </verify>
  <done>Every session-derived message/part mutation route reuses the same session-root guard before calling session mutation functions.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: add part ownership and anonymous fallback regressions</name>
  <files>packages/opencode/test/server/session-part-ownership.test.ts</files>
  <behavior>
    - Test 1: different user gets 404 when deleting another user's message.
    - Test 2: different user gets 404 when deleting or patching another user's part.
    - Test 3: anonymous caller can still read part-bearing data from a legacy anonymous session.
  </behavior>
  <action>Create focused server integration coverage for protected mutation handlers plus one anonymous fallback read case that proves parts remain accessible only through anonymous-owned sessions. Use real API keys and real server requests, keep fixture setup local to the file, and verify the protected data still exists after denied cross-user mutations.</action>
  <verify>
    <automated>bun test test/server/session-messages-ownership.test.ts test/server/session-part-ownership.test.ts --timeout 30000</automated>
  </verify>
  <done>Cross-user message/part mutations are denied with 404, owner data remains intact after denied attempts, and anonymous legacy sessions still work.</done>
</task>

</tasks>

<verification>
Run from `packages/opencode`:
- `bun test test/server/session-messages-ownership.test.ts test/server/session-part-ownership.test.ts --timeout 30000`
</verification>

<success_criteria>

- Shared guard covers all remaining session-derived message/part mutation routes.
- Cross-user delete/update attempts return 404 and do not alter stored data.
- Anonymous legacy sessions still expose only their own part-bearing message data.
  </success_criteria>

<output>
After completion, create `.planning/phases/05-session-derived-ownership-closure/05-02-SUMMARY.md`
</output>
