---
phase: 02-session-ownership
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - packages/opencode/migration/
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04
  - SESS-05
  - SESS-06

must_haves:
  truths:
    - "Migration file exists adding user_id column to session table"
    - "Existing sessions retain NULL user_id after migration"
    - "Type checking passes across the package"
  artifacts:
    - path: "packages/opencode/migration/"
      provides: "Drizzle migration adding user_id to session"
      contains: "ALTER TABLE session ADD COLUMN user_id"
  key_links:
    - from: "packages/opencode/migration/"
      to: "packages/opencode/src/session/session.sql.ts"
      via: "Drizzle Kit schema diff"
      pattern: "user_id"
---

<objective>
Generate Drizzle migration for the user_id column and verify the full phase compiles and migrates cleanly.

Purpose: Produce the migration artifact and confirm all phase changes are coherent
Output: Migration file, passing typecheck
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-session-ownership/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Drizzle migration</name>
  <files>packages/opencode/migration/</files>
  <action>
Run Drizzle Kit migration generation from the opencode package directory:

```bash
cd packages/opencode && bun run db generate --name add_session_user_id
```

This produces `migration/<timestamp>_add_session_user_id/migration.sql` and `snapshot.json`.

Verify the generated SQL contains:

- `ALTER TABLE session ADD COLUMN user_id TEXT` (nullable, no NOT NULL constraint)
- An index on user_id: `CREATE INDEX session_user_idx ON session (user_id)`

If the migration output does not match, inspect `session.sql.ts` for any missing column or index definition and fix before re-running.
</action>
<verify>
<automated>ls packages/opencode/migration/\*add_session_user_id/migration.sql</automated>
</verify>
<done>Migration file exists with user_id column addition</done>
</task>

<task type="auto">
  <name>Task 2: Full typecheck</name>
  <files>packages/opencode/src/session/index.ts</files>
  <action>
Run typecheck from the opencode package to confirm all phase changes compile cleanly:

```bash
cd packages/opencode && bun typecheck
```

Fix any type errors before proceeding. Common issues to watch for:

- UserID type import missing in session.sql.ts
- UserContext import missing in session/index.ts
- `userID` field missing from Info type or toRow/fromRow mappings
- `and()` import needed for multi-condition WHERE clauses (already imported)
  </action>
  <verify>
  <automated>cd packages/opencode && bun typecheck</automated>
  </verify>
  <done>Zero type errors across the package</done>
  </task>

</tasks>

<verification>
- Migration file generated with correct SQL
- user_id column is nullable (backward compatible)
- Index on user_id present
- Full package typecheck passes
</verification>

<success_criteria>

- All SESS-01 through SESS-06 requirements addressed across plans 01-04
- Migration is additive — existing sessions keep NULL user_id
- Single-user deployments continue working (anonymous sees all NULL sessions)
  </success_criteria>

<output>
After completion, create `.planning/phases/02-session-ownership/02-04-SUMMARY.md`
</output>
