---
phase: 03-user-management-api
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/opencode/src/server/user-auth.ts
  - packages/opencode/package.json
autonomous: true
gap_closure: true
requirements: [USER-01, USER-02]
must_haves:
  truths:
    - "bun typecheck passes with zero errors in phase-introduced files"
    - "user-auth.ts resolves the database client via Database.use()"
    - "bcrypt types are available in user/index.ts"
  artifacts:
    - path: "packages/opencode/src/server/user-auth.ts"
      provides: "API key resolution using Database.use() pattern"
      contains: "Database.use"
    - path: "packages/opencode/package.json"
      provides: "@types/bcrypt in devDependencies"
      contains: "@types/bcrypt"
  key_links:
    - from: "packages/opencode/src/server/user-auth.ts"
      to: "packages/opencode/src/storage/db.ts"
      via: "Database.use()"
      pattern: "Database\\.use"
---

<objective>
Fix two type errors introduced in Phase 3 that prevent a clean typecheck.

Purpose: Phase 3 is functionally complete but fails `bun typecheck` due to a bad import in user-auth.ts and missing bcrypt type declarations. These are compile-time gaps only — no logic changes needed.
Output: Zero type errors in all phase-introduced files.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-user-management-api/03-VERIFICATION.md

<interfaces>
<!-- From packages/opencode/src/storage/db.ts -->
```typescript
// Database.use() — the correct pattern for all DB access in this codebase
export namespace Database {
  export function use<T>(callback: (trx: TxOrDb) => T): T
  export type TxOrDb = SQLiteTransaction<"sync", void, any, any> | Client
}

// NOT exported — do not import:
// const db = drizzle(...) — this is internal to Database.Client lazy init

````

<!-- From packages/opencode/src/user/index.ts (existing, correct) -->
```typescript
// Already uses Database.use() correctly:
const row = Database.use((db) => db.select().from(UserTable).where(...).get())
````

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: fix user-auth.ts — replace `db` import with Database.use()</name>
  <files>packages/opencode/src/server/user-auth.ts</files>
  <action>
    Remove `import { db } from '../storage/db'` (line 4) — `db` is never exported from storage/db, only the `Database` namespace is.

    Rewrite the `resolve()` function to use `Database.use()` instead of the bare `db` variable. The existing query `db.select().from(ApiKeyTable).all()` becomes `Database.use((db) => db.select().from(ApiKeyTable).all())`.

    Note: `resolve()` is async but `Database.use()` is sync — the bcrypt `verify()` call happens outside the Database.use() callback (iterate rows after fetching). Keep the same logic, just wrap the select in Database.use().

    The import line should become: `import { Database, eq } from '../storage/db'` (keep `eq` if used, drop it if not — check the file).

    Do NOT change any other logic — throttle(), the return shapes, or the UserContext wiring.

  </action>
  <verify>
    <automated>cd packages/opencode && bun typecheck 2>&1 | grep "user-auth"</automated>
  </verify>
  <done>No type errors referencing user-auth.ts in typecheck output</done>
</task>

<task type="auto">
  <name>task 2: add @types/bcrypt to devDependencies</name>
  <files>packages/opencode/package.json</files>
  <action>
    Add `"@types/bcrypt": "latest"` to the `devDependencies` section of `packages/opencode/package.json`, then run `bun install` from the `packages/opencode` directory to install it.

    This resolves TS7016 on `import { hash, compare } from 'bcrypt'` in user/index.ts — no code changes needed there.

  </action>
  <verify>
    <automated>cd packages/opencode && bun typecheck 2>&1 | grep "bcrypt"</automated>
  </verify>
  <done>No type errors referencing bcrypt in typecheck output</done>
</task>

<task type="auto">
  <name>task 3: full typecheck clean pass</name>
  <files></files>
  <action>
    Run `bun typecheck` from `packages/opencode` and confirm zero errors in the phase-introduced files:
    - packages/opencode/src/server/user-auth.ts
    - packages/opencode/src/user/index.ts
    - packages/opencode/src/user/user.sql.ts
    - packages/opencode/src/user/usage.sql.ts
    - packages/opencode/src/user/usage.ts
    - packages/opencode/src/server/routes/user.ts

    If errors remain in other (pre-existing) files unrelated to Phase 3, note them in the summary but do not fix them — scope is Phase 3 files only.

  </action>
  <verify>
    <automated>cd packages/opencode && bun typecheck 2>&1 | grep -E "user-auth|user/index|user\.sql|usage\.sql|usage\.ts|routes/user" || echo "CLEAN"</automated>
  </verify>
  <done>Zero type errors in all six phase-introduced files</done>
</task>

</tasks>

<verification>
Run from packages/opencode:
- `bun typecheck 2>&1 | grep -E "user-auth|user/index|user\.sql|usage|routes/user"` → no output (clean)
- Confirm user-auth.ts contains `Database.use` and does NOT contain `import { db }`
- Confirm package.json devDependencies contains `@types/bcrypt`
</verification>

<success_criteria>
`bun typecheck` produces zero errors in all Phase 3 files. The `Database.use()` pattern is used consistently in user-auth.ts matching the rest of the codebase.
</success_criteria>

<output>
After completion, create `.planning/phases/03-user-management-api/03-05-SUMMARY.md`
</output>
